// Import mock implementation for development
import * as mock from "./mock";

// Export development mock or production implementation
export const rtConnect = mock.rtConnect;
export const rtFeed = mock.rtFeed;
